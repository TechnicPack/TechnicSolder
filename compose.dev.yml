# Development stack owned by the technicsolder-dev Compose project. Run it on its own; do NOT merge with compose.yml:
#   docker compose -f compose.dev.yml up -d
# Combining the two files (-f compose.yml -f compose.dev.yml) pulls in the
# production MariaDB service and its env-injection block, which breaks the
# postgres dev database and test isolation.
name: technicsolder-dev

services:
  nginx:
    image: nginx
    depends_on:
      - solder
    ports:
      - "8080:80"
    volumes:
      - ./docker/default.conf:/etc/nginx/conf.d/default.conf:ro
      - .:/var/www/html:ro
      - ./public:/var/www/html/public:ro

  solder:
    build:
      context: .
      dockerfile: docker/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - .:/var/www/html
      - vendor:/var/www/html/vendor
    command: ["/bin/bash", "/var/www/html/docker/dev-entrypoint.sh"]

  postgres:
    image: postgres
    # Let host-run SolderJS share this database without exposing it to the LAN.
    # Port 5434 avoids SolderJS's isolated integration-test PostgreSQL on 5433.
    ports:
      - "127.0.0.1:5434:5432"
    environment:
      - POSTGRES_DB=${DB_DATABASE:-solder}
      - POSTGRES_USER=${DB_USERNAME:-solder}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-solder}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U solder"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis
    # Let host-run SolderJS use Redis without exposing it to the LAN.
    # Port 6381 avoids SolderJS's isolated integration-test Redis on 6380.
    ports:
      - "127.0.0.1:6381:6379"

volumes:
  postgres_data:
  vendor:
